#!/usr/bin/env ruby
require 'refinement_builder'
class RefinementBuilder::CLI < Thor
  desc "test", "run tests"
  def test
    puts "No tests have been wrritten"
    exit
  end
end
RefinementBuilder::CLI.start ARGV
